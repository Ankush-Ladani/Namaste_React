// Hot Module Replacement ( HMR )
// Created Server
// File Watcher algorithm ( Whether any file is changed/modified or not?) -> written in C++
// BUNDLING
// MINIFY
// Cleaning our code
// Dev and Production Build
// Super Fast build algorithm
// Image optimization
// Caching while development
// compression
// compatible with older verison of browser
// HTTPS on dev -> npx parcel index.html --https
// manages port Numbers
// Consistent hashing algorithms

// browserlist -> manages your website will be compatible to which browsers
// Ex -> In package.json
// you have to add "browserlist" : {
//      "last 2 versions"
// }

// For Chrome
// "browserlist" : {
//      "last 2 Chrome versions"
// }

// PARCEL -> ZERO CONFIG BUNDLER

// TRANSITIVE DEPENDENCIES -> ( A dependency depends on another and it also depend on other )

// The .cache folder (or .parcel-cache in parcel v2) stores information about your project when parcel builds it, so that when it rebuilds, it doesn't have to re-parse and re-analyze everything from scratch.
